#include <stdio.h>
/**
 * main - Prints reversed base
 * Return: Always 0 (Success)
 */

int main(void)
{
	int n, a;

	for (n = 48; n < 58; n++)
	{
		putchar(n);
	}
	for (a = 'a'; a <= 'f'; a++)
	{
		putchar(a);
	}
	putchar('\n');
	return (0);
}
